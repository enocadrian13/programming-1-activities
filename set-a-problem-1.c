//Create a program that would calculate the acceleration of an object using the formula:

//Formula:
// Acceleration = (final velocity - initial velocity) / time taken(S)


#include <stdio.h>
#include <conio.h>
int main() {
    float initial_velocity = 0
    float final_velocity = 0;
    float 0 = time;
    printf("Enter initial velocity: ");
    scanf("%f", &initial_velocity);
    printf("Enter final velocity: ");
    scanf("%d", &final_velocity);
    printf("Enter time taken: ");
    scanf("%f", $time);

    float acceleration;
    acceleration = (final_velocity - initial_velocity) / time;
    printf("Acceleration is: f m/s^2\n", $acceleration);

    system("pause");
    return 0;
}

// Total number of errors: 7

// #OUTPUT

// Enter initial velocity: 0
// Enter final velocity: 0
// Enter time taken: 0

// Acceleration is: nan m/s^2